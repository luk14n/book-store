databaseChangeLog:
  - changeSet:
      id: update-role-names
      author: lukian
      changes:
        - sql:
            splitStatements: true
            endDelimiter: ';'
            sql: |
              UPDATE roles SET role = CONCAT('ROLE_', role);